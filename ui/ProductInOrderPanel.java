package ui;

import javax.swing.JLabel;
import javax.swing.JPanel;

public class ProductInOrderPanel extends JPanel {
        private JLabel label1;
        private JLabel label2;
        private JLabel label3;
        private JLabel label4;

        // public ProductInOrderPanel() {
        // label1 = new JLabel("Price: " + product.getProduct().getPrice() + " Quantity:
        // " + product.getQuantity()
        // + " Total Price: " + product.getTotalPrice());
        // label2 = new JLabel("Price: " + product.getProduct().getPrice() + " Quantity:
        // " + product.getQuantity()
        // + " Total Price: " + product.getTotalPrice());
        // label3 = new JLabel("Price: " + product.getProduct().getPrice() + " Quantity:
        // " + product.getQuantity()
        // + " Total Price: " + product.getTotalPrice());
        // label4 = new JLabel("Price: " + product.getProduct().getPrice() + " Quantity:
        // " + product.getQuantity()
        // + " Total Price: " + product.getTotalPrice());

        // add(label1);
        // add(label2);
        // add(label3);
        // add(label4);
        // }

        // @Override
        // public void actionPerformed(ActionEvent e) {
        // if (e.getActionCommand().equals("Add Product")) {
        // JPanel productPanel = new JPanel();
        // productPanel.setLayout(new BorderLayout());
        // JButton button1 = new JButton("+ 1");
        // productPanel.add(button1, BorderLayout.WEST);
        // JButton button2 = new JButton("- 1");
        // productPanel.add(button2, BorderLayout.CENTER);
        // JButton button3 = new JButton("Remove");
        // productPanel.add(button3, BorderLayout.EAST);
        // mainPanel.add(productPanel);
        // scrollPane.revalidate();
        // } else if (e.getActionCommand().equals("Exit")) {
        // System.exit(0);
        // }
        // }

}
